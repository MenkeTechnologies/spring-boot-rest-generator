package {{mainPackageName}}.entity

import groovy.transform.*
import java.io.Serializable
import jakarta.persistence.*
import java.time.*

@Entity
@Table(name = "{{tableName}}")
@Canonical
class {{entityName}} implements Serializable {
